[package]
name = "plato-observe"
version = "0.1.0"
edition = "2021"
description = "Observability layer for OpenConstruct — metrics, tracing, health checks, and event bus"
license = "MIT"

[dependencies]
